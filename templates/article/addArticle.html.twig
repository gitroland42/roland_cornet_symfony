{% extends 'base.html.twig' %}

{% block title %}Hello article!{% endblock %}



{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

{# <div class="example-wrapper">
    
    
   <h1> Ajout article</h1> <br> #}

    {# affichage auto des erreurs avec  cette methode #}
    {# {{ form_start(form) }} 
    {{ form_end(form) }}  #}

<div class="container formulaire col-8">
        <div class="darkThemeLighter carte rounded p-4 mt-4">


            <h4 class="blueThemeWrite text-uppercase">Ajout article</h4>
                {{ form_start(form) }}
                <div class="form-group">
                    {{ form_label(form.titre) }}
                    {{ form_widget(form.titre) }}
                </div>
                <br>
                {{ form_end(form) }}


                {% for error in  form.vars.errors.form.getErrors(true) %}
                    <li>{{ error.message }}</li>
                {% endfor %}
        </div>

</div>
{% endblock %}